// Import React if you're using JSX syntax
import React from 'react';

// Define the TableHeader component
function TableHeader() {
 // The TableHeader component returns a JSX element that represents the title of the table
 return (
    // A div element is used to contain the title text
    <div className="text-[#1A181E] text-[20px] font-medium leading-[28px] font-px-regular">
      Toddy's Appointment List
    </div>
 );
}

// Export the TableHeader component
export default TableHeader;
